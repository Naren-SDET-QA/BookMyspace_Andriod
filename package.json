{
  "name": "bookmyspace-firestore-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"node --test firestore.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4"
  }
}
